<!DOCTYPE html>
<html lang="he" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ניהול שואלים - ספרייה</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Assistant:wght@300;400;500&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary-bg: #fefefe;
            --secondary-bg: #f8f9fa;
            --accent: #6c757d;
            --text-primary: #2c3e50;
            --text-secondary: #6c757d;
            --border: #e9ecef;
            --hover: #f1f3f4;
            --success: #d4edda;
            --warning: #fff3cd;
            --danger: #f8d7da;
        }
    
        * {
            box-sizing: border-box;
        }
    
        body {
            font-family: 'Assistant', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
            background-color: var(--primary-bg);
            color: var(--text-primary);
            line-height: 1.6;
            margin: 0;
            padding: 0;
        }
    
        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 2rem 20px;
        }
    
        /* כותרת ראשית */
        .main-header {
            background-color: var(--primary-bg);
            border-bottom: 1px solid var(--border);
            padding: 2rem 0;
            margin-bottom: 2rem;
            text-align: center;
        }
    
        .main-header h2 {
            font-size: 2.2rem;
            font-weight: 300;
            color: var(--text-primary);
            margin-bottom: 1rem;
        }
    
        .main-header i {
            color: var(--accent);
            margin-left: 0.5rem;
        }
    
        /* כפתורי פעולה */
        .action-buttons {
            display: flex;
            gap: 1rem;
            justify-content: center;
            flex-wrap: wrap;
            margin-top: 1.5rem;
        }
    
        .btn-clean {
            padding: 0.75rem 1.5rem;
            border: 1px solid var(--border);
            border-radius: 6px;
            background-color: var(--primary-bg);
            color: var(--text-primary);
            text-decoration: none;
            font-size: 0.95rem;
            transition: all 0.3s ease;
            display: inline-flex;
            align-items: center;
            cursor: pointer;
        }
    
        .btn-clean:hover {
            background-color: var(--hover);
            border-color: var(--accent);
            color: var(--text-primary);
            transform: translateY(-1px);
        }
    
        .btn-primary-clean {
            background-color: var(--text-primary);
            color: var(--primary-bg);
            border-color: var(--text-primary);
        }
    
        .btn-primary-clean:hover {
            background-color: var(--accent);
            color: var(--primary-bg);
        }
    
        /* חיפוש */
        .search-section {
            max-width: 500px;
            margin: 0 auto 3rem auto;
        }
    
        .search-input {
            width: 100%;
            padding: 1rem 1.5rem;
            border: 1px solid var(--border);
            border-radius: 8px;
            font-size: 1rem;
            background-color: var(--primary-bg);
            transition: border-color 0.3s ease;
        }
    
        .search-input:focus {
            outline: none;
            border-color: var(--accent);
        }
    
        /* רשת שואלים */
        .borrowers-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
            gap: 1.5rem;
        }
    
        .borrower-card-clean {
            background-color: var(--primary-bg);
            border: 1px solid var(--border);
            border-radius: 12px;
            overflow: hidden;
            transition: all 0.3s ease;
            box-shadow: 0 2px 8px rgba(0,0,0,0.04);
        }
    
        .borrower-card-clean:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 16px rgba(0,0,0,0.08);
            border-color: var(--accent);
        }
    
        .card-header-clean {
            background-color: var(--secondary-bg);
            padding: 1.5rem;
            border-bottom: 1px solid var(--border);
        }
    
        .card-header-clean h5 {
            margin: 0;
            font-size: 1.25rem;
            font-weight: 500;
            color: var(--text-primary);
        }
    
        .card-body-clean {
            padding: 1.5rem;
        }
    
        .borrower-info {
            margin-bottom: 1rem;
        }
    
        .borrower-info p {
            margin: 0.5rem 0;
            font-size: 0.95rem;
            color: var(--text-secondary);
        }
    
        .borrower-info strong {
            color: var(--text-primary);
        }
    
        /* סטטוס ספרים */
        .status-alert {
            padding: 0.75rem;
            border-radius: 6px;
            font-size: 0.9rem;
            margin-bottom: 1rem;
            display: flex;
            align-items: center;
        }
    
        .status-success {
            background-color: var(--success);
            color: #155724;
            border: 1px solid #c3e6cb;
        }
    
        .status-warning {
            background-color: var(--warning);
            color: #856404;
            border: 1px solid #ffeaa7;
        }
    
        /* כפתורי פעולות בכרטיס */
        .card-actions {
            padding: 1rem 1.5rem;
            background-color: var(--secondary-bg);
            border-top: 1px solid var(--border);
            display: flex;
            gap: 0.5rem;
        }
    
        .btn-sm-clean {
            padding: 0.5rem 1rem;
            font-size: 0.875rem;
            border-radius: 4px;
            border: 1px solid var(--border);
            background-color: var(--primary-bg);
            color: var(--text-primary);
            text-decoration: none;
            transition: all 0.3s ease;
            flex: 1;
            text-align: center;
            cursor: pointer;
        }
    
        .btn-sm-clean:hover {
            background-color: var(--hover);
            color: var(--text-primary);
        }
    
        .btn-danger-clean {
            background-color: var(--danger);
            color: #721c24;
            border-color: #f5c6cb;
        }
    
        .btn-danger-clean:hover {
            background-color: #f1b0b7;
            color: #721c24;
        }
    
        /* מצב ריק */
        .empty-state {
            text-align: center;
            padding: 4rem 2rem;
            color: var(--text-secondary);
        }
    
        .empty-state i {
            font-size: 4rem;
            margin-bottom: 1.5rem;
            color: var(--accent);
        }
    
        .empty-state h4 {
            margin-bottom: 1rem;
            color: var(--text-primary);
        }
    
        /* רספונסיביות */
        @media (max-width: 768px) {
            .container {
                padding: 1rem 15px;
            }
            
            .main-header h2 {
                font-size: 1.8rem;
            }
            
            .borrowers-grid {
                grid-template-columns: 1fr;
            }
            
            .action-buttons {
                flex-direction: column;
                align-items: center;
            }
            
            .card-actions {
                flex-direction: column;
            }
        }
    </style>
    
</head>
<body>
    <div class="container">
        <!-- כותרת ראשית -->
        <header class="main-header">
            <h2><i class="fas fa-users"></i>ניהול שואלים</h2>
            <div class="action-buttons">
                <a href="/borrowers/add" class="btn-clean btn-primary-clean">
                    <i class="fas fa-plus" style="margin-left: 0.5rem;"></i>הוסף שואל חדש
                </a>
                <button class="btn-clean" onclick="document.getElementById('searchInput').focus()">
                    <i class="fas fa-search" style="margin-left: 0.5rem;"></i>חיפוש
                </button>
            </div>
        </header>

        <!-- חיפוש -->
        <div class="search-section">
            <input type="text" id="searchInput" class="search-input" placeholder="חפש לפי שם או אימייל...">
        </div>

        <!-- רשימת שואלים -->
        <div class="borrowers-grid" id="borrowersList">
            <% if (typeof borrowers !== 'undefined' && borrowers.length > 0) { %>
                <% borrowers.forEach(borrower => { %>
                    <div class="borrower-item">
                        <div class="borrower-card-clean">
                            <div class="card-header-clean">
                                <h5>
                                    <i class="fas fa-user" style="margin-left: 0.5rem; color: var(--accent);"></i>
                                    <%= borrower.name || 'ללא שם' %>
                                </h5>
                            </div>
                            <div class="card-body-clean">
                                <div class="borrower-info">
                                    <p><strong><i class="fas fa-envelope" style="margin-left: 0.5rem;"></i> אימייל:</strong> <%= borrower.email || 'לא צוין' %></p>
                                    <p><strong><i class="fas fa-phone" style="margin-left: 0.5rem;"></i> טלפון:</strong> <%= borrower.phone || 'לא צוין' %></p>
                                    <p><strong><i class="fas fa-map-marker-alt" style="margin-left: 0.5rem;"></i> כתובת:</strong> <%= borrower.address || 'לא צוינה' %></p>
                                </div>
                                
                                <% if (borrower.borrowedBooks && borrower.borrowedBooks.length > 0) { %>
                                    <div class="status-alert status-warning">
                                        <i class="fas fa-book" style="margin-left: 0.5rem;"></i>
                                        ספרים שאולים: <%= borrower.borrowedBooks.length %>
                                    </div>
                                <% } else { %>
                                    <div class="status-alert status-success">
                                        <i class="fas fa-check" style="margin-left: 0.5rem;"></i>
                                        אין ספרים שאולים
                                    </div>
                                <% } %>
                            </div>
                            <div class="card-actions">
                                <a href="/borrowers/<%= borrower._id %>" class="btn-sm-clean">
                                    <i class="fas fa-eye"></i> צפה
                                </a>
                                <a href="/borrowers/update/<%= borrower._id %>" class="btn-sm-clean">
                                    <i class="fas fa-edit"></i> ערוך
                                </a>
                                <button onclick="deleteBorrower('<%= borrower._id %>')" class="btn-sm-clean btn-danger-clean">
                                    <i class="fas fa-trash"></i> מחק
                                </button>
                            </div>
                        </div>
                    </div>
                <% }) %>
            <% } else { %>
                <div style="grid-column: 1 / -1;">
                    <div class="empty-state">
                        <i class="fas fa-users"></i>
                        <h4>אין שואלים רשומים במערכת</h4>
                        <p>התחל בהוספת השואל הראשון שלך</p>
                        <a href="/borrowers/add" class="btn-clean btn-primary-clean">
                            <i class="fas fa-plus" style="margin-left: 0.5rem;"></i>הוסף שואל ראשון
                        </a>
                    </div>
                </div>
            <% } %>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // חיפוש שואלים
        document.getElementById('searchInput').addEventListener('input', function() {
            const searchTerm = this.value.toLowerCase();
            const borrowerItems = document.querySelectorAll('.borrower-item');
            
            borrowerItems.forEach(item => {
                const text = item.textContent.toLowerCase();
                if (text.includes(searchTerm)) {
                    item.style.display = 'block';
                } else {
                    item.style.display = 'none';
                }
            });
        });

        // מחיקת שואל
        function deleteBorrower(id) {
            if (confirm('האם אתה בטוח שברצונך למחוק את השואל? פעולה זו לא ניתנת לביטול.')) {
                fetch(`/borrowers/delete/${id}`, {
                    method: 'DELETE'
                }).then(response => {
                    if (response.ok) {
                        location.reload();
                    } else {
                        alert('שגיאה במחיקת השואל');
                    }
                }).catch(error => {
                    console.error('Error:', error);
                    alert('שגיאה במחיקת השואל');
                });
            }
        }
    </script>
</body>

</html>
